package need_for_speed;

public class DriverD extends Driver<Bus> {
    public DriverD(String fullNameDriver, Character driverLicense, double experience, String category,Bus bus) {
        super(fullNameDriver, driverLicense, experience,category, bus);
    }
}
